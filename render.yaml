# Render Blueprint: подключи репозиторий на render.com → Blueprint → Apply
services:
  - type: web
    name: signalos
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SIGNALOS_PLATFORM_KEY
        sync: false
